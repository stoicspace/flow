{
  "name": "flowlens",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:desktop": "npm run dev --workspace=apps/desktop",
    "build:web": "npm run build --workspace=apps/web",
    "build:core": "npm run build --workspace=packages/core",
    "build:desktop": "npm run build --workspace=apps/desktop",
    "build:packages": "npm run build --workspace=packages/core; npm run build --workspace=packages/storage-sqlite; npm run build --workspace=packages/keystore-electron; npm run build --workspace=packages/storage-supabase",
    "package:desktop": "npm run package --workspace=apps/desktop"
  }
}